services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: issuing
      POSTGRES_USER: issuing
      POSTGRES_PASSWORD: issuing
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
